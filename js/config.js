/*
 * Налаштування дослідження.
 * Єдиний файл, який треба відредагувати після розгортання Apps Script.
 */
window.PSYLAB_CONFIG = {

  /* Адреса веб-застосунку Apps Script (Deploy → Web app → URL).
     Поки поле порожнє, сайт працює в демонстраційному режимі:
     все рахується і показується, але дані нікуди не надсилаються. */
  appsScriptUrl: '',

  /* Версія ключів підрахунку. Збільшуйте щоразу, коли змінюєте
     ключі або склад шкал у methods/*.json — значення пишеться в таблицю
     і дозволяє потім відрізнити записи, пораховані різними версіями. */
  scoringVersion: '1.0.0',

  /* Порядок проходження методик. Імена відповідають файлам у methods/. */
  methods: ['debq', 'bsq34', 'tas20', 'stai'],

  /* Скільки тверджень показувати на одному екрані. */
  itemsPerPage: 8,

  /* Скільки разів повторити спробу надсилання, якщо мережа підвела. */
  submitRetries: 3
};
